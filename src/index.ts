// OpenWebAI - 入口文件
// MVP 阶段：组装 Gateway + Scheduler + Driver + Adapter

async function main() {
  console.log('OpenWebAI starting...');
  // TODO: Phase 6 实现
}

main().catch((err) => {
  console.error('Fatal error:', err);
  process.exit(1);
});
